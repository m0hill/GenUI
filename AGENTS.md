## Start here

`docs/README.md` is the documentation index and the entry point for the
project's guides. The old design docs (`docs/genui/`) were deleted
deliberately; do not resurrect them from git history.

When creating or editing any Markdown, follow `docs/documentation.md`, and keep
`docs/README.md` current.
