## Start here

Read `ROADMAP.md` before doing any work. It is the single source of truth for
the project goal, architecture, milestone order, testing philosophy, commit
rules, and hard scope guards. The old design docs (`docs/genui/`) were deleted
deliberately; do not resurrect them from git history.

## Vendored Repositories (inert)

`repos/` contains vendored Datastar sources that were reference material for
the deleted chat example. Nothing in the workspace uses them anymore. Treat
the directory as inert: do not read it for guidance, do not edit it, and do
not import from it. It is kept only until the owner removes it.