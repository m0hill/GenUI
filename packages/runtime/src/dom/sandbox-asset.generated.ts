// @generated by scripts/build-sandbox-asset.mjs
// Do not edit by hand.
const sandboxRuntimeAssetChunks = [
  '"use strict";\n(() => {\n  // src/dialect/genui0-language.ts\n  var createGenui0Language = () => {\n    const capabilityName',
  'PatternSource = "[a-z][a-z0-9]*(?:[._-][a-z0-9]+)+";\n    const bareIdentifierPatternSource = "_?[A-Za-z][A-Za-z0-9_]*";\n',
  '    const statePathPatternSource = "\\\\$_?[A-Za-z][A-Za-z0-9_]*(?:\\\\._?[A-Za-z][A-Za-z0-9_]*)*";\n    const numberLiteralP',
  'atternSource = "-?(?:0|[1-9]\\\\d*)(?:\\\\.\\\\d+)?";\n    const stringLiteralPatternSource = `(?:"[^"\\\\\\\\<>]*"|\'[^\'\\\\\\\\<>]*\')`',
  ";\n    const exactPattern = (source, flags) => new RegExp(`^${source}$`, flags);\n    const capabilityNamePattern = exactP",
  'attern(capabilityNamePatternSource, "i");\n    const bareIdentifierPattern = exactPattern(bareIdentifierPatternSource);\n ',
  "   const statePathPattern = exactPattern(statePathPatternSource);\n    const numberLiteralPattern = exactPattern(numberLi",
  "teralPatternSource);\n    const stringLiteralPattern = exactPattern(stringLiteralPatternSource);\n    const invalid = /* @",
  '__PURE__ */ Symbol("genui0.invalid");\n    const splitOutsideQuotes = (source, separator) => {\n      const parts = [];\n  ',
  "    let quote;\n      let start = 0;\n      for (let index = 0; index < source.length; index += 1) {\n        const charact",
  'er = source[index];\n        if (character === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n    ',
  "    if (quote !== void 0) {\n          if (character === quote) quote = void 0;\n          continue;\n        }\n        if ",
  '(character === \'"\' || character === "\'") {\n          quote = character;\n          continue;\n        }\n        if ("()[]{',
  '}".includes(character)) return void 0;\n        if (character === separator) {\n          parts.push(source.slice(start, i',
  "ndex).trim());\n          start = index + 1;\n        }\n      }\n      if (quote !== void 0) return void 0;\n      parts.pus",
  "h(source.slice(start).trim());\n      return parts.every((part) => part.length > 0) ? parts : void 0;\n    };\n    const sp",
  "litTopLevel = (source, separator) => {\n      const parts = [];\n      let quote;\n      let depth = 0;\n      let start = 0",
  ";\n      for (let index = 0; index < source.length; index += 1) {\n        const character = source[index];\n        if (ch",
  'aracter === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n        if (quote !== void 0) {\n      ',
  "    if (character === quote) quote = void 0;\n          continue;\n        }\n        if (character === '\"' || character ==",
  '= "\'") {\n          quote = character;\n          continue;\n        }\n        if (character === "(" || character === "[" |',
  '| character === "{") {\n          depth += 1;\n          continue;\n        }\n        if (character === ")" || character ==',
  '= "]" || character === "}") {\n          depth -= 1;\n          if (depth < 0) return void 0;\n          continue;\n        ',
  "}\n        if (character === separator && depth === 0) {\n          parts.push(source.slice(start, index).trim());\n       ",
  "   start = index + 1;\n        }\n      }\n      if (quote !== void 0 || depth !== 0) return void 0;\n      parts.push(sourc",
  "e.slice(start).trim());\n      return parts.every((part) => part.length > 0) ? parts : void 0;\n    };\n    const splitKeyV",
  'alue = (entry) => {\n      const parts = splitOutsideQuotes(entry, ":");\n      return parts?.length === 2 && parts[0] !==',
  " void 0 && parts[1] !== void 0 ? [parts[0], parts[1]] : void 0;\n    };\n    const splitComparison = (source) => {\n      l",
  "et quote;\n      for (let index = 0; index < source.length - 1; index += 1) {\n        const character = source[index];\n  ",
  '      if (character === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n        if (quote !== void',
  " 0) {\n          if (character === quote) quote = void 0;\n          continue;\n        }\n        if (character === '\"' || ",
  'character === "\'") {\n          quote = character;\n          continue;\n        }\n        const operator = source.slice(in',
  'dex, index + 2);\n        if (operator === "==" || operator === "!=") {\n          const left = source.slice(0, index).tri',
  "m();\n          const right = source.slice(index + 2).trim();\n          return left.length > 0 && right.length > 0 ? [lef",
  "t, operator, right] : void 0;\n        }\n      }\n      return void 0;\n    };\n    const stringLiteralValue = (value) => {\n",
  "      const source = value.trim();\n      return stringLiteralPattern.test(source) ? source.slice(1, -1) : invalid;\n    }",
  ";\n    const objectKeyName = (value) => {\n      const source = value.trim();\n      if (bareIdentifierPattern.test(source)",
  ") return source;\n      const literal = stringLiteralValue(source);\n      return literal !== invalid && bareIdentifierPat",
  "tern.test(literal) ? literal : invalid;\n    };\n    const isCapabilityName = (value) => capabilityNamePattern.test(value)",
  ";\n    const isStateName = (value) => bareIdentifierPattern.test(value);\n    const isStatePath = (value) => statePathPatt",
  'ern.test(value);\n    const statePathParts = (value) => {\n      const source = value.startsWith("$") ? value.slice(1) : v',
  'alue;\n      const parts = source.split(".");\n      return parts.length > 0 && parts.every(isStateName) ? parts : void 0;',
  "\n    };\n    const parseScalarExpression = (source, readState) => {\n      const value = source.trim();\n      if (stringLi",
  "teralPattern.test(value)) return value.slice(1, -1);\n      if (numberLiteralPattern.test(value)) return Number(value);\n ",
  '     if (value === "true") return true;\n      if (value === "false") return false;\n      if (value === "null") return nu',
  "ll;\n      if (statePathPattern.test(value)) return readState(value);\n      return invalid;\n    };\n    const isSafeScalar",
  'Expression = (value) => parseScalarExpression(value, () => "") !== invalid;\n    const parseObjectLiteral = (source, read',
  'State) => {\n      const value = source.trim();\n      if (!value.startsWith("{") || !value.endsWith("}")) return invalid;',
  "\n      const body = value.slice(1, -1).trim();\n      if (body.length === 0) return {};\n      const entries = splitOutsid",
  'eQuotes(body, ",");\n      if (entries === void 0) return invalid;\n      const output = {};\n      for (const entry of ent',
  "ries) {\n        const keyValue = splitKeyValue(entry);\n        if (keyValue === void 0) return invalid;\n        const ke",
  "y = objectKeyName(keyValue[0]);\n        if (key === invalid) return invalid;\n        const parsedValue = parseScalarExpr",
  "ession(keyValue[1], readState);\n        if (parsedValue === invalid) return invalid;\n        output[key] = parsedValue;\n",
  '      }\n      return output;\n    };\n    const isSafeObjectExpression = (value) => parseObjectLiteral(value, () => "") !=',
  "= invalid;\n    const evaluateExpression = (source, readState) => {\n      const value = source.trim();\n      const compar",
  "ison = splitComparison(value);\n      if (comparison !== void 0) {\n        const left = parseScalarExpression(comparison[",
  "0], readState);\n        const right = parseScalarExpression(comparison[2], readState);\n        if (left === invalid || r",
  'ight === invalid) return invalid;\n        return comparison[1] === "==" ? Object.is(left, right) : !Object.is(left, righ',
  "t);\n      }\n      const scalar = parseScalarExpression(value, readState);\n      if (scalar !== invalid) return scalar;\n ",
  "     return parseObjectLiteral(value, readState);\n    };\n    const isSafeSimpleExpression = (value) => value.length <= 1",
  '200 && evaluateExpression(value, () => "") !== invalid;\n    const isSafeBindingExpression = (value) => {\n      const sou',
  "rce = value.trim();\n      return source.length <= 1200 && (isStateName(source) || isStatePath(source));\n    };\n    const",
  ' parseTargetOptions = (value) => {\n      const source = value.trim();\n      if (!source.startsWith("{") || !source.endsW',
  'ith("}")) return void 0;\n      const body = source.slice(1, -1).trim();\n      if (body.length === 0) return void 0;\n    ',
  '  const entries = splitOutsideQuotes(body, ",");\n      if (entries === void 0 || entries.length !== 1) return void 0;\n  ',
  "    const keyValue = splitKeyValue(entries[0]);\n      if (keyValue === void 0) return void 0;\n      const [key, target] ",
  '= keyValue;\n      const targetValue = stringLiteralValue(target);\n      return objectKeyName(key) === "target" && target',
  "Value !== invalid && isStateName(targetValue) ? targetValue : void 0;\n    };\n    const parseCapabilityAction = (value) =",
  '> {\n      const source = value.trim();\n      const prefix = "@capability(";\n      if (!source.startsWith(prefix) || !sou',
  'rce.endsWith(")")) return void 0;\n      const args = splitTopLevel(source.slice(prefix.length, -1), ",");\n      if (args',
  " === void 0 || args.length !== 2 && args.length !== 3) return void 0;\n      const capability = stringLiteralValue(args[0",
  "]);\n      const inputExpression = args[1];\n      if (capability === invalid || inputExpression === void 0 || !isCapabili",
  "tyName(capability) || !isSafeObjectExpression(inputExpression)) {\n        return void 0;\n      }\n      if (args[2] === v",
  "oid 0) return { capability, inputExpression };\n      const target = parseTargetOptions(args[2]);\n      return target ===",
  " void 0 ? void 0 : { capability, inputExpression, target };\n    };\n    const parseCapabilityExpression = (expression, re",
  "adState) => {\n      const action = parseCapabilityAction(expression);\n      if (action === void 0) return void 0;\n      ",
  "const input = parseObjectLiteral(action.inputExpression, readState);\n      if (input === invalid) return void 0;\n      r",
  "eturn action.target === void 0 ? { capability: action.capability, input } : { capability: action.capability, input, targ",
  "et: action.target };\n    };\n    const parseSetAction = (value) => {\n      const source = value.trim();\n      const prefi",
  'x = "@set(";\n      if (!source.startsWith(prefix) || !source.endsWith(")")) return void 0;\n      const args = splitTopLe',
  'vel(source.slice(prefix.length, -1), ",");\n      if (args === void 0 || args.length !== 2) return void 0;\n      const pa',
  "thExpression = stringLiteralValue(args[0]);\n      const valueExpression = args[1];\n      if (pathExpression === invalid ",
  "|| valueExpression === void 0 || statePathParts(pathExpression) === void 0 || !isSafeSimpleExpression(valueExpression)) ",
  "{\n        return void 0;\n      }\n      return { pathExpression, valueExpression };\n    };\n    const parseSetExpression =",
  " (expression, readState) => {\n      const action = parseSetAction(expression);\n      if (action === void 0) return void ",
  "0;\n      const path = statePathParts(action.pathExpression);\n      if (path === void 0) return void 0;\n      const value",
  " = evaluateExpression(action.valueExpression, readState);\n      return value === invalid ? void 0 : { path, value };\n   ",
  ' };\n    const camelCaseWords = (words) => {\n      const [first, ...rest] = words;\n      if (first === void 0) return "ca',
  'pability";\n      return [\n        first.toLowerCase(),\n        ...rest.map((word) => `${word.slice(0, 1).toUpperCase()}$',
  '{word.slice(1).toLowerCase()}`)\n      ].join("");\n    };\n    const defaultResultTarget = (capability) => {\n      const w',
  "ords = capability.split(/[._-]+/).filter((part) => part.length > 0);\n      const target = camelCaseWords(words);\n      r",
  'eturn isStateName(target) ? target : "capability";\n    };\n    const normalizeResultTarget = (target, capability) => targ',
  "et !== void 0 && isStateName(target) ? target : defaultResultTarget(capability);\n    return {\n      invalid,\n      isCap",
  "abilityName,\n      isStateName,\n      isStatePath,\n      isSafeScalarExpression,\n      isSafeObjectExpression,\n      isS",
  "afeSimpleExpression,\n      isSafeBindingExpression,\n      parseCapabilityAction,\n      parseSetAction,\n      parseObject",
  "Literal,\n      evaluateExpression,\n      parseCapabilityExpression,\n      parseSetExpression,\n      defaultResultTarget,",
  "\n      normalizeResultTarget\n    };\n  };\n  var genui0Language = createGenui0Language();\n\n  // src/css-style.ts\n  var all",
  'owedStylePropertyList = [\n    "align-content",\n    "align-items",\n    "align-self",\n    "aspect-ratio",\n    "background"',
  ',\n    "background-color",\n    "background-image",\n    "background-position",\n    "background-size",\n    "border",\n    "b',
  'order-bottom",\n    "border-bottom-color",\n    "border-bottom-left-radius",\n    "border-bottom-right-radius",\n    "border',
  '-bottom-style",\n    "border-bottom-width",\n    "border-collapse",\n    "border-color",\n    "border-left",\n    "border-lef',
  't-color",\n    "border-left-style",\n    "border-left-width",\n    "border-radius",\n    "border-right",\n    "border-right-c',
  'olor",\n    "border-right-style",\n    "border-right-width",\n    "border-spacing",\n    "border-style",\n    "border-top",\n ',
  '   "border-top-color",\n    "border-top-left-radius",\n    "border-top-right-radius",\n    "border-top-style",\n    "border-',
  'top-width",\n    "border-width",\n    "bottom",\n    "box-shadow",\n    "box-sizing",\n    "color",\n    "column-gap",\n    "cu',
  'rsor",\n    "display",\n    "flex",\n    "flex-basis",\n    "flex-direction",\n    "flex-grow",\n    "flex-shrink",\n    "flex-',
  'wrap",\n    "font",\n    "font-family",\n    "font-size",\n    "font-style",\n    "font-weight",\n    "gap",\n    "grid-templat',
  'e-columns",\n    "grid-template-rows",\n    "height",\n    "inset",\n    "justify-content",\n    "justify-items",\n    "justif',
  'y-self",\n    "left",\n    "letter-spacing",\n    "line-height",\n    "list-style",\n    "list-style-position",\n    "list-sty',
  'le-type",\n    "margin",\n    "margin-bottom",\n    "margin-left",\n    "margin-right",\n    "margin-top",\n    "max-height",\n',
  '    "max-width",\n    "min-height",\n    "min-width",\n    "object-fit",\n    "object-position",\n    "opacity",\n    "outline',
  '",\n    "outline-color",\n    "outline-offset",\n    "outline-style",\n    "outline-width",\n    "overflow",\n    "overflow-wr',
  'ap",\n    "overflow-x",\n    "overflow-y",\n    "padding",\n    "padding-bottom",\n    "padding-left",\n    "padding-right",\n ',
  '   "padding-top",\n    "place-content",\n    "place-items",\n    "pointer-events",\n    "position",\n    "right",\n    "row-ga',
  'p",\n    "text-align",\n    "text-decoration",\n    "text-transform",\n    "top",\n    "transform",\n    "vertical-align",\n   ',
  ' "visibility",\n    "white-space",\n    "width",\n    "z-index"\n  ];\n  var propertyPattern = /^(?:--[a-z0-9-]+|[a-z][a-z0-9',
  "-]*)$/;\n  var unsafeValuePattern = /(?:url|image-set|cross-fade|element|paint|expression)\\s*\\(|(?:javascript|vbscript|da",
  "ta)\\s*:|@import|-moz-binding|behavior\\s*:/i;\n  var allowedStyleProperties = new Set(allowedStylePropertyList);\n  var has",
  "UnsafeStyleCharacter = (value) => {\n    for (let index = 0; index < value.length; index += 1) {\n      const character = ",
  'value[index];\n      const code = value.charCodeAt(index);\n      if (character === "<" || character === ">" || character ',
  '=== "\\\\" || code === 127 || code <= 8 || code === 11 || code === 12 || code >= 14 && code <= 31) {\n        return true;\n',
  "      }\n    }\n    return false;\n  };\n  var normalizeStylePropertyName = (property) => property.trim().toLowerCase();\n  v",
  "ar normalizeGenuiStylePropertyName = (attributeSuffix) => {\n    const property = normalizeStylePropertyName(attributeSuf",
  'fix);\n    return property.startsWith("property-") ? property.slice("property-".length) : property;\n  };\n  var isSafeStyl',
  "eProperty = (property) => {\n    const normalized = normalizeStylePropertyName(property);\n    return propertyPattern.test",
  "(normalized) && allowedStyleProperties.has(normalized);\n  };\n  var isSafeStyleValue = (value) => {\n    const normalized ",
  "= value.trim();\n    return normalized.length > 0 && normalized.length <= 512 && !hasUnsafeStyleCharacter(normalized) && ",
  '!unsafeValuePattern.test(normalized) && !normalized.includes("/*") && !normalized.includes("*/");\n  };\n\n  // src/dom/san',
  "dbox-runtime.ts\n  var installSandboxRuntime = (config2, language, global) => {\n    let nextCallId = 1;\n    let resizeObs",
  "erver;\n    const state = {};\n    const boundElements = [];\n    const directives = [];\n    const eachBlocks = [];\n    con",
  "st elementScopes = /* @__PURE__ */ new WeakMap();\n    const emptyScope = {};\n    const isRecord = (value) => typeof valu",
  'e === "object" && value !== null && !Array.isArray(value);\n    const hasOwn = (value, key) => Object.prototype.hasOwnPro',
  'perty.call(value, key);\n    const readOwnProperty = (value, key) => {\n      if (typeof value !== "object" || value === n',
  "ull) return { found: false };\n      const descriptor = Object.getOwnPropertyDescriptor(value, key);\n      return descrip",
  'tor !== void 0 && "value" in descriptor ? { found: true, value: descriptor.value } : { found: false };\n    };\n    const ',
  'statePath = (expression) => {\n      const source = expression.startsWith("$") ? expression.slice(1) : expression;\n      ',
  'return source.split(".").filter((part) => part.length > 0);\n    };\n    const readPath = (path, scope = emptyScope) => {\n',
  '      const [name, ...rest] = path;\n      if (name === void 0) return "";\n      let value = hasOwn(scope, name) ? scope[',
  'name] : hasOwn(state, name) ? state[name] : "";\n      for (const property of rest) {\n        const next = readOwnPropert',
  'y(value, property);\n        if (!next.found) return "";\n        value = next.value;\n      }\n      return value;\n    };\n ',
  "   const writePath = (path, value) => {\n      const [name, ...rest] = path;\n      if (name === void 0) return;\n      if ",
  "(rest.length === 0) {\n        state[name] = value;\n        return;\n      }\n      let cursor;\n      const current = state",
  "[name];\n      if (isRecord(current)) {\n        cursor = current;\n      } else {\n        cursor = {};\n        state[name]",
  " = cursor;\n      }\n      for (const property of rest.slice(0, -1)) {\n        const child = cursor[property];\n        if ",
  "(isRecord(child)) {\n          cursor = child;\n          continue;\n        }\n        const next = {};\n        cursor[prop",
  "erty] = next;\n        cursor = next;\n      }\n      const last = rest.at(-1);\n      if (last !== void 0) cursor[last] = v",
  "alue;\n    };\n    const readStateFromScope = (scope = emptyScope) => (expression) => readPath(statePath(expression), scop",
  "e);\n    const evaluate = (expression, scope = emptyScope) => language.evaluateExpression(expression, readStateFromScope(",
  "scope));\n    const isTruthy = (value) => value !== language.invalid && value !== false && value !== null && value !== vo",
  'id 0 && value !== "" && value !== 0;\n    const textValue = (value) => {\n      if (value === language.invalid || value ==',
  '= null || value === void 0) return "";\n      if (typeof value === "string") return value;\n      if (typeof value === "nu',
  'mber" || typeof value === "boolean") return String(value);\n      return JSON.stringify(value);\n    };\n    const post = (',
  "message) => {\n      global.parent.postMessage(\n        { channel: config2.channel, surfaceId: config2.surfaceId, ...mess",
  'age },\n        "*"\n      );\n    };\n    const createCallId = () => global.crypto !== void 0 && typeof global.crypto.rando',
  'mUUID === "function" ? global.crypto.randomUUID() : `call-${nextCallId++}`;\n    const readElementValue = (element) => {\n',
  "      if (global.HTMLInputElement !== void 0 && element instanceof global.HTMLInputElement) {\n        const type = eleme",
  'nt.type.toLowerCase();\n        if (type === "checkbox") return element.checked;\n        if (type === "radio") return ele',
  'ment.checked ? element.value : "";\n        if ((type === "number" || type === "range") && element.value !== "") {\n      ',
  "    const numberValue = Number(element.value);\n          return Number.isFinite(numberValue) ? numberValue : element.val",
  "ue;\n        }\n        return element.value;\n      }\n      if (global.HTMLSelectElement !== void 0 && element instanceof ",
  "global.HTMLSelectElement) {\n        if (element.multiple) return Array.from(element.selectedOptions).map((option) => opt",
  "ion.value);\n        return element.value;\n      }\n      if (global.HTMLTextAreaElement !== void 0 && element instanceof ",
  'global.HTMLTextAreaElement) {\n        return element.value;\n      }\n      return element.textContent ?? "";\n    };\n    c',
  "onst writeElementValue = (element, value) => {\n      if (global.HTMLInputElement !== void 0 && element instanceof global",
  '.HTMLInputElement) {\n        const type = element.type.toLowerCase();\n        if (type === "checkbox") {\n          eleme',
  "nt.checked = Boolean(value);\n          return;\n        }\n        element.value = textValue(value);\n        return;\n     ",
  " }\n      if (global.HTMLSelectElement !== void 0 && element instanceof global.HTMLSelectElement) {\n        const values ",
  "= Array.isArray(value) ? value.map(textValue) : [textValue(value)];\n        for (const option of element.options) option",
  ".selected = values.includes(option.value);\n        return;\n      }\n      if (global.HTMLTextAreaElement !== void 0 && el",
  "ement instanceof global.HTMLTextAreaElement) {\n        element.value = textValue(value);\n        return;\n      }\n    };\n",
  "    const hasAuthoredFormValue = (element) => {\n      if (global.HTMLInputElement !== void 0 && element instanceof globa",
  'l.HTMLInputElement) {\n        const type = element.type.toLowerCase();\n        return type === "checkbox" || type === "r',
  'adio" ? element.hasAttribute("checked") : element.hasAttribute("value");\n      }\n      if (global.HTMLTextAreaElement !=',
  "= void 0 && element instanceof global.HTMLTextAreaElement) {\n        return element.textContent !== null && element.text",
  "Content.length > 0;\n      }\n      if (global.HTMLSelectElement !== void 0 && element instanceof global.HTMLSelectElement",
  ') {\n        return Array.from(element.options).some((option) => option.hasAttribute("selected"));\n      }\n      return f',
  "alse;\n    };\n    const closestWithAttribute = (target, attributeName) => {\n      let element = target instanceof global.",
  "Element ? target : null;\n      while (element !== null) {\n        if (element.hasAttribute(attributeName)) return elemen",
  "t;\n        element = element.parentElement;\n      }\n      return null;\n    };\n    const isBoundElement = (target) => tar",
  'get instanceof global.Element && target.hasAttribute("data-genui-bind");\n    const safeDynamicAttribute = (attribute) =>',
  ' {\n      const name = attribute.toLowerCase();\n      if (name.startsWith("on")) return false;\n      if (name.startsWith(',
  '"aria-")) return true;\n      return ["role", "title", "disabled", "checked", "value"].includes(name);\n    };\n    const e',
  "lementStyle = (element) => {\n      return element.style;\n    };\n    const applyAttributeValue = (element, attribute, val",
  "ue) => {\n      if (!safeDynamicAttribute(attribute)) return;\n      if (value === language.invalid || value === false || ",
  "value === null || value === void 0) {\n        element.removeAttribute(attribute);\n        return;\n      }\n      element.",
  'setAttribute(attribute, value === true ? "" : textValue(value));\n    };\n    const applyStyleValue = (element, property, ',
  "value) => {\n      const style = elementStyle(element);\n      const normalizedProperty = normalizeGenuiStylePropertyName(",
  "property);\n      if (style === void 0 || !isSafeStyleProperty(normalizedProperty)) return;\n      if (value === language.",
  "invalid || value === false || value === null || value === void 0) {\n        style.removeProperty(normalizedProperty);\n  ",
  "      return;\n      }\n      const styleValue = textValue(value);\n      if (!isSafeStyleValue(styleValue)) {\n        styl",
  "e.removeProperty(normalizedProperty);\n        return;\n      }\n      style.setProperty(normalizedProperty, styleValue);\n ",
  "   };\n    const currentDirectives = () => [\n      ...directives,\n      ...eachBlocks.flatMap((block) => block.directives",
  ")\n    ];\n    const refreshDirectives = () => {\n      for (const directive of currentDirectives()) {\n        const value ",
  '= evaluate(directive.expression, directive.scope);\n        if (directive.type === "text") {\n          directive.element.',
  'textContent = textValue(value);\n          continue;\n        }\n        if (directive.type === "show") {\n          const s',
  "tyle = elementStyle(directive.element);\n          if (style !== void 0) style.display = isTruthy(value) ? directive.visi",
  'bleDisplay : "none";\n          continue;\n        }\n        if (directive.type === "class_toggle") {\n          directive.',
  "element.classList.toggle(directive.className, isTruthy(value));\n          continue;\n        }\n        if (directive.type",
  ' === "class_value") {\n          const dynamicClass = typeof value === "string" ? value.trim() : "";\n          directive.',
  "element.className = dynamicClass.length === 0 ? directive.baseClassName : [directive.baseClassName, dynamicClass].filter",
  '((item) => item.length > 0).join(" ");\n          continue;\n        }\n        if (directive.type === "style_property") {\n',
  "          applyStyleValue(directive.element, directive.property, value);\n          continue;\n        }\n        if (direc",
  'tive.type === "style_map") {\n          if (isRecord(value)) {\n            for (const [property, propertyValue] of Object',
  ".entries(value)) {\n              applyStyleValue(directive.element, property, propertyValue);\n            }\n          }\n",
  "          continue;\n        }\n        applyAttributeValue(directive.element, directive.attribute, value);\n      }\n    };",
  "\n    const reportHeight = () => {\n      const root = global.document.documentElement;\n      const body = global.document",
  '.body;\n      post({ type: "resize", height: Math.max(root.scrollHeight, body?.scrollHeight ?? 0) });\n    };\n    const re',
  "fresh = () => {\n      renderEachBlocks();\n      refreshDirectives();\n      reportHeight();\n    };\n    const resultTarget",
  "For = (action) => action.target ?? language.defaultResultTarget(action.capability);\n    const setResultState = (target, ",
  "state2) => {\n      global.__genuiResults = global.__genuiResults ?? {};\n      global.__genuiResults[target] = state2;\n  ",
  "    writePath([target], state2);\n    };\n    const pendingResultState = (target) => {\n      const previous = readPath([ta",
  'rget]);\n      return isRecord(previous) && hasOwn(previous, "value") ? { status: "pending", value: previous.value } : { ',
  'status: "pending" };\n    };\n    const postCapabilityCall = (expression, scope) => {\n      const action = language.parseC',
  "apabilityExpression(expression, readStateFromScope(scope));\n      if (action === void 0) return false;\n      const targe",
  "t = resultTargetFor(action);\n      setResultState(target, pendingResultState(target));\n      refresh();\n      post({\n   ",
  '     type: "capability",\n        callId: createCallId(),\n        capability: action.capability,\n        input: action.in',
  "put,\n        ...action.target === void 0 ? {} : { target: action.target }\n      });\n      return true;\n    };\n    const ",
  "runLocalAction = (expression, scope) => {\n      const action = language.parseSetExpression(expression, readStateFromScop",
  "e(scope));\n      if (action === void 0) return false;\n      writePath(action.path, action.value);\n      refresh();\n     ",
  " return true;\n    };\n    const scopeForElement = (element) => {\n      let current = element;\n      while (current !== nu",
  "ll) {\n        const scope = elementScopes.get(current);\n        if (scope !== void 0) return scope;\n        current = cu",
  "rrent.parentElement;\n      }\n      return emptyScope;\n    };\n    const runAuthoredAction = (expression, scope) => runLoc",
  "alAction(expression, scope) || postCapabilityCall(expression, scope);\n    const handleClick = (event) => {\n      const a",
  'ction = closestWithAttribute(event.target, "data-genui-on-click");\n      const expression = action?.getAttribute("data-g',
  'enui-on-click") ?? null;\n      if (action !== null && expression !== null && runAuthoredAction(expression, scopeForEleme',
  "nt(action))) {\n        event.preventDefault();\n        return;\n      }\n      const target = event.target;\n      if (!(ta",
  'rget instanceof global.Element)) return;\n      const link = target.closest("a[href]");\n      if (link === null) return;\n',
  "      const href = global.HTMLAnchorElement !== void 0 && link instanceof global.HTMLAnchorElement ? link.href : link.ge",
  'tAttribute("href");\n      if (href === null) return;\n      event.preventDefault();\n      post({ type: "link", href });\n ',
  "   };\n    const handleSubmit = (event) => {\n      const target = event.target;\n      if (!(target instanceof global.Elem",
  'ent)) return;\n      const form = target.closest("form");\n      if (form === null || !form.hasAttribute("data-genui-on-su',
  'bmit")) return;\n      event.preventDefault();\n      const expression = form.getAttribute("data-genui-on-submit");\n      ',
  "if (expression !== null) runAuthoredAction(expression, scopeForElement(form));\n    };\n    const handleBoundInput = (even",
  "t) => {\n      const target = event.target;\n      if (!isBoundElement(target)) return;\n      writePath(statePath(target.g",
  'etAttribute("data-genui-bind") ?? ""), readElementValue(target));\n      refresh();\n    };\n    const handleResultMessage ',
  "= (event) => {\n      const message = event.data;\n      if (!isRecord(message)) return;\n      if (message.channel !== con",
  'fig2.channel || message.surfaceId !== config2.surfaceId) return;\n      if (message.type !== "result" || typeof message.t',
  'arget !== "string") return;\n      setResultState(message.target, message.state);\n      refresh();\n    };\n    const insta',
  'llInitialState = () => {\n      for (const element of global.document.querySelectorAll("[data-genui-state]")) {\n        c',
  'onst parsed = language.parseObjectLiteral(\n          element.getAttribute("data-genui-state") ?? "{}",\n          readSta',
  "teFromScope(emptyScope)\n        );\n        if (!isRecord(parsed)) continue;\n        for (const [name, value] of Object.e",
  "ntries(parsed)) state[name] = value;\n      }\n    };\n    const installBindings = () => {\n      for (const element of glob",
  'al.document.querySelectorAll("[data-genui-bind]")) {\n        const path = statePath(element.getAttribute("data-genui-bin',
  'd") ?? "");\n        if (path.length === 0) continue;\n        boundElements.push({ element, path });\n        if (hasAutho',
  'redFormValue(element) || readPath(path) === "") {\n          writePath(path, readElementValue(element));\n        } else {',
  "\n          writeElementValue(element, readPath(path));\n        }\n      }\n    };\n    const installEachBlocks = () => {\n  ",
  "    const isNestedEachBlock = (element) => {\n        let parent = element.parentElement;\n        while (parent !== null)",
  ' {\n          if (parent.hasAttribute("data-genui-each")) return true;\n          parent = parent.parentElement;\n        }',
  '\n        return false;\n      };\n      const elements = Array.from(global.document.querySelectorAll("[data-genui-each]"))',
  ".filter(\n        (element) => !isNestedEachBlock(element)\n      );\n      for (const element of elements) {\n        const",
  ' expression = element.getAttribute("data-genui-each");\n        if (expression === null) continue;\n        const itemName',
  ' = element.getAttribute("data-genui-as") ?? "item";\n        const template = Array.from(element.childNodes).map((node) =',
  "> node.cloneNode(true));\n        element.replaceChildren();\n        eachBlocks.push({ element, expression, itemName, tem",
  "plate, directives: [] });\n      }\n    };\n    const installDirective = (element, attribute, scope, targetDirectives) => {",
  '\n      const { name, value } = attribute;\n      if (name === "data-genui-text") {\n        targetDirectives.push({ type: ',
  '"text", element, expression: value, scope });\n        return;\n      }\n      if (name === "data-genui-show") {\n        co',
  'nst visibleDisplay = elementStyle(element)?.display ?? "";\n        targetDirectives.push({ type: "show", element, expres',
  'sion: value, visibleDisplay, scope });\n        return;\n      }\n      if (name === "data-genui-class") {\n        targetDi',
  'rectives.push({\n          type: "class_value",\n          element,\n          baseClassName: element.className,\n          ',
  'expression: value,\n          scope\n        });\n        return;\n      }\n      if (name.startsWith("data-genui-class-")) {',
  '\n        targetDirectives.push({\n          type: "class_toggle",\n          element,\n          className: name.slice("dat',
  'a-genui-class-".length),\n          expression: value,\n          scope\n        });\n        return;\n      }\n      if (name',
  ' === "data-genui-style") {\n        targetDirectives.push({ type: "style_map", element, expression: value, scope });\n    ',
  '    return;\n      }\n      if (name.startsWith("data-genui-style-")) {\n        targetDirectives.push({\n          type: "s',
  'tyle_property",\n          element,\n          property: normalizeGenuiStylePropertyName(name.slice("data-genui-style-".le',
  'ngth)),\n          expression: value,\n          scope\n        });\n        return;\n      }\n      if (name.startsWith("data',
  '-genui-attr-")) {\n        targetDirectives.push({\n          type: "attribute",\n          element,\n          attribute: n',
  'ame.slice("data-genui-attr-".length),\n          expression: value,\n          scope\n        });\n      }\n    };\n    const ',
  "installDirectives = (root, scope, targetDirectives) => {\n      const elements = root instanceof global.Element ? [root, ",
  '...Array.from(root.querySelectorAll("*"))] : Array.from(root.querySelectorAll("*"));\n      for (const element of element',
  "s) {\n        for (const attribute of element.attributes) {\n          installDirective(element, attribute, scope, targetD",
  "irectives);\n        }\n      }\n    };\n    const renderElementTree = (element, scope, targetDirectives) => {\n      element",
  "Scopes.set(element, scope);\n      for (const attribute of element.attributes) {\n        installDirective(element, attrib",
  'ute, scope, targetDirectives);\n      }\n      if (element.hasAttribute("data-genui-each")) {\n        renderEachElement(el',
  "ement, scope, targetDirectives);\n        return;\n      }\n      for (const child of element.children) renderElementTree(c",
  "hild, scope, targetDirectives);\n    };\n    const renderEachTemplate = (element, expression, itemName, template, parentSc",
  "ope, targetDirectives) => {\n      element.replaceChildren();\n      const items = evaluate(expression, parentScope);\n    ",
  "  if (!Array.isArray(items)) return;\n      for (const item of items) {\n        const itemScope = { ...parentScope, [item",
  "Name]: item };\n        for (const templateNode of template) {\n          const clone = templateNode.cloneNode(true);\n    ",
  "      element.append(clone);\n          if (clone instanceof global.Element) renderElementTree(clone, itemScope, targetDi",
  "rectives);\n        }\n      }\n    };\n    function renderEachElement(element, scope, targetDirectives) {\n      const expre",
  'ssion = element.getAttribute("data-genui-each");\n      if (expression === null) return;\n      const itemName = element.g',
  'etAttribute("data-genui-as") ?? "item";\n      const template = Array.from(element.childNodes).map((node) => node.cloneNo',
  "de(true));\n      renderEachTemplate(element, expression, itemName, template, scope, targetDirectives);\n    }\n    const r",
  "enderEachBlocks = () => {\n      for (const block of eachBlocks) {\n        block.directives = [];\n        renderEachTempl",
  "ate(\n          block.element,\n          block.expression,\n          block.itemName,\n          block.template,\n          ",
  "emptyScope,\n          block.directives\n        );\n      }\n    };\n    installInitialState();\n    installEachBlocks();\n   ",
  " installBindings();\n    installDirectives(global.document, emptyScope, directives);\n    renderEachBlocks();\n    refreshD",
  'irectives();\n    global.addEventListener("load", reportHeight);\n    global.document.addEventListener("click", handleClic',
  'k);\n    global.document.addEventListener("submit", handleSubmit);\n    global.document.addEventListener("input", handleBo',
  'undInput);\n    global.document.addEventListener("change", handleBoundInput);\n    global.addEventListener("message", hand',
  "leResultMessage);\n    if (global.ResizeObserver !== void 0 && global.document.body !== null) {\n      resizeObserver = ne",
  "w global.ResizeObserver(reportHeight);\n      resizeObserver.observe(global.document.body);\n    }\n    return {\n      disp",
  'ose() {\n        resizeObserver?.disconnect();\n        global.removeEventListener("load", reportHeight);\n        global.d',
  'ocument.removeEventListener("click", handleClick);\n        global.document.removeEventListener("submit", handleSubmit);\n',
  '        global.document.removeEventListener("input", handleBoundInput);\n        global.document.removeEventListener("cha',
  'nge", handleBoundInput);\n        global.removeEventListener("message", handleResultMessage);\n      }\n    };\n  };\n\n  // s',
  "rc/dom/sandbox-entry.ts\n  var sandboxGlobal = globalThis;\n  var config = sandboxGlobal.__genuiSandboxConfig;\n  delete sa",
  'ndboxGlobal.__genuiSandboxConfig;\n  if (config === void 0) {\n    throw new Error("Missing GenUI sandbox config.");\n  }\n ',
  " var genui0Language2 = createGenui0Language();\n  installSandboxRuntime(\n    config,\n    {\n      invalid: genui0Language2",
  ".invalid,\n      parseObjectLiteral: (source, readState) => genui0Language2.parseObjectLiteral(source, readState),\n      ",
  "evaluateExpression: (source, readState) => genui0Language2.evaluateExpression(source, readState),\n      parseCapabilityE",
  "xpression: (expression, readState) => genui0Language2.parseCapabilityExpression(expression, readState),\n      parseSetEx",
  "pression: (expression, readState) => genui0Language2.parseSetExpression(expression, readState),\n      defaultResultTarge",
  "t: (capability) => genui0Language2.defaultResultTarget(capability)\n    },\n    sandboxGlobal\n  );\n})();\n",
] as const

export const sandboxRuntimeAsset = sandboxRuntimeAssetChunks.join("")
