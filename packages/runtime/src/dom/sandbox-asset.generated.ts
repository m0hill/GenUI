// @generated by scripts/build-sandbox-asset.mjs
// Do not edit by hand.
const sandboxRuntimeAssetChunks = [
  '"use strict";\n(() => {\n  // src/dialect/genui0-language.ts\n  var createGenui0Language = () => {\n    const capabilityName',
  'PatternSource = "[a-z][a-z0-9]*(?:[._-][a-z0-9]+)+";\n    const bareIdentifierPatternSource = "_?[A-Za-z][A-Za-z0-9_]*";\n',
  '    const statePathPatternSource = "\\\\$_?[A-Za-z][A-Za-z0-9_]*(?:\\\\._?[A-Za-z][A-Za-z0-9_]*)*";\n    const numberLiteralP',
  'atternSource = "-?(?:0|[1-9]\\\\d*)(?:\\\\.\\\\d+)?";\n    const stringLiteralPatternSource = `(?:"[^"\\\\\\\\<>]*"|\'[^\'\\\\\\\\<>]*\')`',
  ";\n    const exactPattern = (source, flags) => new RegExp(`^${source}$`, flags);\n    const capabilityNamePattern = exactP",
  'attern(capabilityNamePatternSource, "i");\n    const bareIdentifierPattern = exactPattern(bareIdentifierPatternSource);\n ',
  "   const statePathPattern = exactPattern(statePathPatternSource);\n    const numberLiteralPattern = exactPattern(numberLi",
  "teralPatternSource);\n    const stringLiteralPattern = exactPattern(stringLiteralPatternSource);\n    const invalid = /* @",
  '__PURE__ */ Symbol("genui0.invalid");\n    const splitOutsideQuotes = (source, separator) => {\n      const parts = [];\n  ',
  "    let quote;\n      let start = 0;\n      for (let index = 0; index < source.length; index += 1) {\n        const charact",
  'er = source[index];\n        if (character === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n    ',
  "    if (quote !== void 0) {\n          if (character === quote) quote = void 0;\n          continue;\n        }\n        if ",
  '(character === \'"\' || character === "\'") {\n          quote = character;\n          continue;\n        }\n        if ("()[]{',
  '}".includes(character)) return void 0;\n        if (character === separator) {\n          parts.push(source.slice(start, i',
  "ndex).trim());\n          start = index + 1;\n        }\n      }\n      if (quote !== void 0) return void 0;\n      parts.pus",
  "h(source.slice(start).trim());\n      return parts.every((part) => part.length > 0) ? parts : void 0;\n    };\n    const sp",
  "litTopLevel = (source, separator) => {\n      const parts = [];\n      let quote;\n      let depth = 0;\n      let start = 0",
  ";\n      for (let index = 0; index < source.length; index += 1) {\n        const character = source[index];\n        if (ch",
  'aracter === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n        if (quote !== void 0) {\n      ',
  "    if (character === quote) quote = void 0;\n          continue;\n        }\n        if (character === '\"' || character ==",
  '= "\'") {\n          quote = character;\n          continue;\n        }\n        if (character === "(" || character === "[" |',
  '| character === "{") {\n          depth += 1;\n          continue;\n        }\n        if (character === ")" || character ==',
  '= "]" || character === "}") {\n          depth -= 1;\n          if (depth < 0) return void 0;\n          continue;\n        ',
  "}\n        if (character === separator && depth === 0) {\n          parts.push(source.slice(start, index).trim());\n       ",
  "   start = index + 1;\n        }\n      }\n      if (quote !== void 0 || depth !== 0) return void 0;\n      parts.push(sourc",
  "e.slice(start).trim());\n      return parts.every((part) => part.length > 0) ? parts : void 0;\n    };\n    const splitKeyV",
  'alue = (entry) => {\n      const parts = splitOutsideQuotes(entry, ":");\n      return parts?.length === 2 && parts[0] !==',
  " void 0 && parts[1] !== void 0 ? [parts[0], parts[1]] : void 0;\n    };\n    const splitComparison = (source) => {\n      l",
  "et quote;\n      for (let index = 0; index < source.length - 1; index += 1) {\n        const character = source[index];\n  ",
  '      if (character === void 0) return void 0;\n        if (character === "\\\\") return void 0;\n        if (quote !== void',
  " 0) {\n          if (character === quote) quote = void 0;\n          continue;\n        }\n        if (character === '\"' || ",
  'character === "\'") {\n          quote = character;\n          continue;\n        }\n        const operator = source.slice(in',
  'dex, index + 2);\n        if (operator === "==" || operator === "!=") {\n          const left = source.slice(0, index).tri',
  "m();\n          const right = source.slice(index + 2).trim();\n          return left.length > 0 && right.length > 0 ? [lef",
  "t, operator, right] : void 0;\n        }\n      }\n      return void 0;\n    };\n    const stringLiteralValue = (value) => {\n",
  "      const source = value.trim();\n      return stringLiteralPattern.test(source) ? source.slice(1, -1) : invalid;\n    }",
  ";\n    const objectKeyName = (value) => {\n      const source = value.trim();\n      if (bareIdentifierPattern.test(source)",
  ") return source;\n      const literal = stringLiteralValue(source);\n      return literal !== invalid && bareIdentifierPat",
  "tern.test(literal) ? literal : invalid;\n    };\n    const isCapabilityName = (value) => capabilityNamePattern.test(value)",
  ";\n    const isStateName = (value) => bareIdentifierPattern.test(value);\n    const isStatePath = (value) => statePathPatt",
  'ern.test(value);\n    const statePathParts = (value) => {\n      const source = value.startsWith("$") ? value.slice(1) : v',
  'alue;\n      const parts = source.split(".");\n      return parts.length > 0 && parts.every(isStateName) ? parts : void 0;',
  "\n    };\n    const parseScalarExpression = (source, readState) => {\n      const value = source.trim();\n      if (stringLi",
  "teralPattern.test(value)) return value.slice(1, -1);\n      if (numberLiteralPattern.test(value)) return Number(value);\n ",
  '     if (value === "true") return true;\n      if (value === "false") return false;\n      if (value === "null") return nu',
  "ll;\n      if (statePathPattern.test(value)) return readState(value);\n      return invalid;\n    };\n    const isSafeScalar",
  'Expression = (value) => parseScalarExpression(value, () => "") !== invalid;\n    const parseObjectLiteral = (source, read',
  'State) => {\n      const value = source.trim();\n      if (!value.startsWith("{") || !value.endsWith("}")) return invalid;',
  "\n      const body = value.slice(1, -1).trim();\n      if (body.length === 0) return {};\n      const entries = splitOutsid",
  'eQuotes(body, ",");\n      if (entries === void 0) return invalid;\n      const output = {};\n      for (const entry of ent',
  "ries) {\n        const keyValue = splitKeyValue(entry);\n        if (keyValue === void 0) return invalid;\n        const ke",
  "y = objectKeyName(keyValue[0]);\n        if (key === invalid) return invalid;\n        const parsedValue = parseScalarExpr",
  "ession(keyValue[1], readState);\n        if (parsedValue === invalid) return invalid;\n        output[key] = parsedValue;\n",
  '      }\n      return output;\n    };\n    const isSafeObjectExpression = (value) => parseObjectLiteral(value, () => "") !=',
  "= invalid;\n    const evaluateExpression = (source, readState) => {\n      const value = source.trim();\n      const compar",
  "ison = splitComparison(value);\n      if (comparison !== void 0) {\n        const left = parseScalarExpression(comparison[",
  "0], readState);\n        const right = parseScalarExpression(comparison[2], readState);\n        if (left === invalid || r",
  'ight === invalid) return invalid;\n        return comparison[1] === "==" ? Object.is(left, right) : !Object.is(left, righ',
  "t);\n      }\n      const scalar = parseScalarExpression(value, readState);\n      if (scalar !== invalid) return scalar;\n ",
  "     return parseObjectLiteral(value, readState);\n    };\n    const isSafeSimpleExpression = (value) => value.length <= 1",
  '200 && evaluateExpression(value, () => "") !== invalid;\n    const isSafeBindingExpression = (value) => {\n      const sou',
  "rce = value.trim();\n      return source.length <= 1200 && (isStateName(source) || isStatePath(source));\n    };\n    const",
  ' parseTargetOptions = (value) => {\n      const source = value.trim();\n      if (!source.startsWith("{") || !source.endsW',
  'ith("}")) return void 0;\n      const body = source.slice(1, -1).trim();\n      if (body.length === 0) return void 0;\n    ',
  '  const entries = splitOutsideQuotes(body, ",");\n      if (entries === void 0 || entries.length !== 1) return void 0;\n  ',
  "    const keyValue = splitKeyValue(entries[0]);\n      if (keyValue === void 0) return void 0;\n      const [key, target] ",
  '= keyValue;\n      const targetValue = stringLiteralValue(target);\n      return objectKeyName(key) === "target" && target',
  "Value !== invalid && isStateName(targetValue) ? targetValue : void 0;\n    };\n    const parseCapabilityAction = (value) =",
  '> {\n      const source = value.trim();\n      const prefix = "@capability(";\n      if (!source.startsWith(prefix) || !sou',
  'rce.endsWith(")")) return void 0;\n      const args = splitTopLevel(source.slice(prefix.length, -1), ",");\n      if (args',
  " === void 0 || args.length !== 2 && args.length !== 3) return void 0;\n      const capability = stringLiteralValue(args[0",
  "]);\n      const inputExpression = args[1];\n      if (capability === invalid || inputExpression === void 0 || !isCapabili",
  "tyName(capability) || !isSafeObjectExpression(inputExpression)) {\n        return void 0;\n      }\n      if (args[2] === v",
  "oid 0) return { capability, inputExpression };\n      const target = parseTargetOptions(args[2]);\n      return target ===",
  " void 0 ? void 0 : { capability, inputExpression, target };\n    };\n    const parseCapabilityExpression = (expression, re",
  "adState) => {\n      const action = parseCapabilityAction(expression);\n      if (action === void 0) return void 0;\n      ",
  "const input = parseObjectLiteral(action.inputExpression, readState);\n      if (input === invalid) return void 0;\n      r",
  "eturn action.target === void 0 ? { capability: action.capability, input } : { capability: action.capability, input, targ",
  "et: action.target };\n    };\n    const parseSetAction = (value) => {\n      const source = value.trim();\n      const prefi",
  'x = "@set(";\n      if (!source.startsWith(prefix) || !source.endsWith(")")) return void 0;\n      const args = splitTopLe',
  'vel(source.slice(prefix.length, -1), ",");\n      if (args === void 0 || args.length !== 2) return void 0;\n      const pa',
  "thExpression = stringLiteralValue(args[0]);\n      const valueExpression = args[1];\n      if (pathExpression === invalid ",
  "|| valueExpression === void 0 || statePathParts(pathExpression) === void 0 || !isSafeSimpleExpression(valueExpression)) ",
  "{\n        return void 0;\n      }\n      return { pathExpression, valueExpression };\n    };\n    const parseSetExpression =",
  " (expression, readState) => {\n      const action = parseSetAction(expression);\n      if (action === void 0) return void ",
  "0;\n      const path = statePathParts(action.pathExpression);\n      if (path === void 0) return void 0;\n      const value",
  " = evaluateExpression(action.valueExpression, readState);\n      return value === invalid ? void 0 : { path, value };\n   ",
  ' };\n    const camelCaseWords = (words) => {\n      const [first, ...rest] = words;\n      if (first === void 0) return "ca',
  'pability";\n      return [\n        first.toLowerCase(),\n        ...rest.map((word) => `${word.slice(0, 1).toUpperCase()}$',
  '{word.slice(1).toLowerCase()}`)\n      ].join("");\n    };\n    const defaultResultTarget = (capability) => {\n      const w',
  "ords = capability.split(/[._-]+/).filter((part) => part.length > 0);\n      const target = camelCaseWords(words);\n      r",
  'eturn isStateName(target) ? target : "capability";\n    };\n    const normalizeResultTarget = (target, capability) => targ',
  "et !== void 0 && isStateName(target) ? target : defaultResultTarget(capability);\n    return {\n      invalid,\n      isCap",
  "abilityName,\n      isStateName,\n      isStatePath,\n      isSafeScalarExpression,\n      isSafeObjectExpression,\n      isS",
  "afeSimpleExpression,\n      isSafeBindingExpression,\n      parseCapabilityAction,\n      parseSetAction,\n      parseObject",
  "Literal,\n      evaluateExpression,\n      parseCapabilityExpression,\n      parseSetExpression,\n      defaultResultTarget,",
  "\n      normalizeResultTarget\n    };\n  };\n  var genui0Language = createGenui0Language();\n\n  // src/css-style.ts\n  var all",
  'owedStylePropertyList = [\n    "align-content",\n    "align-items",\n    "align-self",\n    "aspect-ratio",\n    "background"',
  ',\n    "background-color",\n    "background-image",\n    "background-position",\n    "background-size",\n    "border",\n    "b',
  'order-bottom",\n    "border-bottom-color",\n    "border-bottom-left-radius",\n    "border-bottom-right-radius",\n    "border',
  '-bottom-style",\n    "border-bottom-width",\n    "border-collapse",\n    "border-color",\n    "border-left",\n    "border-lef',
  't-color",\n    "border-left-style",\n    "border-left-width",\n    "border-radius",\n    "border-right",\n    "border-right-c',
  'olor",\n    "border-right-style",\n    "border-right-width",\n    "border-spacing",\n    "border-style",\n    "border-top",\n ',
  '   "border-top-color",\n    "border-top-left-radius",\n    "border-top-right-radius",\n    "border-top-style",\n    "border-',
  'top-width",\n    "border-width",\n    "bottom",\n    "box-shadow",\n    "box-sizing",\n    "color",\n    "column-gap",\n    "cu',
  'rsor",\n    "display",\n    "flex",\n    "flex-basis",\n    "flex-direction",\n    "flex-grow",\n    "flex-shrink",\n    "flex-',
  'wrap",\n    "font",\n    "font-family",\n    "font-size",\n    "font-style",\n    "font-weight",\n    "gap",\n    "grid-templat',
  'e-columns",\n    "grid-template-rows",\n    "height",\n    "inset",\n    "justify-content",\n    "justify-items",\n    "justif',
  'y-self",\n    "left",\n    "letter-spacing",\n    "line-height",\n    "list-style",\n    "list-style-position",\n    "list-sty',
  'le-type",\n    "margin",\n    "margin-bottom",\n    "margin-left",\n    "margin-right",\n    "margin-top",\n    "max-height",\n',
  '    "max-width",\n    "min-height",\n    "min-width",\n    "object-fit",\n    "object-position",\n    "opacity",\n    "outline',
  '",\n    "outline-color",\n    "outline-offset",\n    "outline-style",\n    "outline-width",\n    "overflow",\n    "overflow-wr',
  'ap",\n    "overflow-x",\n    "overflow-y",\n    "padding",\n    "padding-bottom",\n    "padding-left",\n    "padding-right",\n ',
  '   "padding-top",\n    "place-content",\n    "place-items",\n    "pointer-events",\n    "position",\n    "right",\n    "row-ga',
  'p",\n    "text-align",\n    "text-decoration",\n    "text-transform",\n    "top",\n    "transform",\n    "vertical-align",\n   ',
  ' "visibility",\n    "white-space",\n    "width",\n    "z-index"\n  ];\n  var propertyPattern = /^(?:--[a-z0-9-]+|[a-z][a-z0-9',
  "-]*)$/;\n  var unsafeValuePattern = /(?:url|image-set|cross-fade|element|paint|expression)\\s*\\(|(?:javascript|vbscript|da",
  "ta)\\s*:|@import|-moz-binding|behavior\\s*:/i;\n  var allowedStyleProperties = new Set(allowedStylePropertyList);\n  var has",
  "UnsafeStyleCharacter = (value) => {\n    for (let index = 0; index < value.length; index += 1) {\n      const character = ",
  'value[index];\n      const code = value.charCodeAt(index);\n      if (character === "<" || character === ">" || character ',
  '=== "\\\\" || code === 127 || code <= 8 || code === 11 || code === 12 || code >= 14 && code <= 31) {\n        return true;\n',
  "      }\n    }\n    return false;\n  };\n  var normalizeStylePropertyName = (property) => property.trim().toLowerCase();\n  v",
  "ar normalizeGenuiStylePropertyName = (attributeSuffix) => {\n    const property = normalizeStylePropertyName(attributeSuf",
  'fix);\n    return property.startsWith("property-") ? property.slice("property-".length) : property;\n  };\n  var isSafeStyl',
  "eProperty = (property) => {\n    const normalized = normalizeStylePropertyName(property);\n    return propertyPattern.test",
  "(normalized) && allowedStyleProperties.has(normalized);\n  };\n  var isSafeStyleValue = (value) => {\n    const normalized ",
  "= value.trim();\n    return normalized.length > 0 && normalized.length <= 512 && !hasUnsafeStyleCharacter(normalized) && ",
  '!unsafeValuePattern.test(normalized) && !normalized.includes("/*") && !normalized.includes("*/");\n  };\n\n  // src/dialect',
  '/genui0.ts\n  var genui0AttributeNames = {\n    state: "data-genui-state",\n    bind: "data-genui-bind",\n    onClick: "data',
  '-genui-on-click",\n    onSubmit: "data-genui-on-submit",\n    show: "data-genui-show",\n    text: "data-genui-text",\n    ea',
  'ch: "data-genui-each",\n    as: "data-genui-as",\n    class: "data-genui-class",\n    classPrefix: "data-genui-class-",\n   ',
  ' style: "data-genui-style",\n    stylePrefix: "data-genui-style-",\n    attrPrefix: "data-genui-attr-"\n  };\n  var safeDyna',
  'micAttribute = (attribute) => {\n    const name = attribute.toLowerCase();\n    if (name.startsWith("aria-")) return true;',
  '\n    return ["role", "title", "disabled", "checked", "value"].includes(name);\n  };\n  var genui0DirectiveDefinitions = [\n',
  '    {\n      key: "state",\n      pattern: { type: "exact", name: genui0AttributeNames.state },\n      usage: genui0Attribu',
  'teNames.state,\n      instruction: "Use data-genui-state for initial local state with a flat object literal.",\n      valu',
  'eKind: "object"\n    },\n    {\n      key: "bind",\n      pattern: { type: "exact", name: genui0AttributeNames.bind },\n     ',
  ' usage: genui0AttributeNames.bind,\n      instruction: "Use data-genui-bind for form state outside repeated rows; do not ',
  'put data-genui-bind inside data-genui-each.",\n      valueKind: "binding",\n      forbiddenInRepeatedTemplate: true\n    },',
  '\n    {\n      key: "on_click",\n      pattern: { type: "exact", name: genui0AttributeNames.onClick },\n      usage: genui0A',
  'ttributeNames.onClick,\n      instruction: "Use data-genui-on-click for @capability(...) and @set(...) actions.",\n      v',
  'alueKind: "action"\n    },\n    {\n      key: "on_submit",\n      pattern: { type: "exact", name: genui0AttributeNames.onSub',
  'mit },\n      usage: genui0AttributeNames.onSubmit,\n      instruction: "Use data-genui-on-submit on forms; native submiss',
  'ion is always prevented.",\n      valueKind: "action"\n    },\n    {\n      key: "show",\n      pattern: { type: "exact", nam',
  'e: genui0AttributeNames.show },\n      usage: genui0AttributeNames.show,\n      instruction: "Use data-genui-show to show ',
  'or hide an authored element from state.",\n      valueKind: "simple",\n      renderable: (_match, value) => ({ type: "show',
  '", expression: value })\n    },\n    {\n      key: "text",\n      pattern: { type: "exact", name: genui0AttributeNames.text ',
  '},\n      usage: genui0AttributeNames.text,\n      instruction: "Use data-genui-text to write dynamic text; do not use tex',
  't interpolation.",\n      valueKind: "simple",\n      renderable: (_match, value) => ({ type: "text", expression: value })',
  '\n    },\n    {\n      key: "each",\n      pattern: { type: "exact", name: genui0AttributeNames.each },\n      usage: genui0A',
  'ttributeNames.each,\n      instruction: "Render arrays with data-genui-each on a container; its existing children are the',
  ' item template.",\n      valueKind: "simple",\n      startsRepeatedTemplate: true\n    },\n    {\n      key: "as",\n      patt',
  'ern: { type: "exact", name: genui0AttributeNames.as },\n      usage: genui0AttributeNames.as,\n      instruction: \'Use dat',
  'a-genui-as="order" to read each repeated item as $order.\',\n      valueKind: "state_name"\n    },\n    {\n      key: "class_',
  'value",\n      pattern: { type: "exact", name: genui0AttributeNames.class },\n      usage: genui0AttributeNames.class,\n   ',
  '   instruction: "Use data-genui-class for a full dynamic class string from state.",\n      valueKind: "simple",\n      ren',
  'derable: (_match, value) => ({ type: "class_value", expression: value })\n    },\n    {\n      key: "class_toggle",\n      p',
  'attern: { type: "prefix", prefix: genui0AttributeNames.classPrefix },\n      usage: "data-genui-class-{class-name}",\n    ',
  '  instruction: "Use data-genui-class-{class-name} to toggle one kebab-case class from state.",\n      valueKind: "simple"',
  ',\n      renderable: (match, value) => match.suffix === void 0 ? void 0 : { type: "class_toggle", className: match.suffix',
  ', expression: value }\n    },\n    {\n      key: "style_map",\n      pattern: { type: "exact", name: genui0AttributeNames.st',
  'yle },\n      usage: genui0AttributeNames.style,\n      instruction: "Use data-genui-style with an object expression for m',
  'ultiple safe style values.",\n      valueKind: "simple",\n      renderable: (_match, value) => ({ type: "style_map", expre',
  'ssion: value })\n    },\n    {\n      key: "style_property",\n      pattern: { type: "prefix", prefix: genui0AttributeNames.',
  'stylePrefix },\n      usage: "data-genui-style-{css-property}",\n      instruction: "Use data-genui-style-{css-property} o',
  'nly when one safe style value must come from state, e.g. data-genui-style-background-color.",\n      valueKind: "simple",',
  "\n      validateName: (match) => match.suffix !== void 0 && isSafeStyleProperty(normalizeGenuiStylePropertyName(match.suf",
  'fix)),\n      renderable: (match, value) => match.suffix === void 0 ? void 0 : {\n        type: "style_property",\n        ',
  'property: normalizeGenuiStylePropertyName(match.suffix),\n        expression: value\n      }\n    },\n    {\n      key: "attr',
  'ibute",\n      pattern: { type: "prefix", prefix: genui0AttributeNames.attrPrefix },\n      usage: "data-genui-attr-{attri',
  'bute}",\n      instruction: "Use data-genui-attr-{attribute} only for safe dynamic attributes: aria-*, role, title, disab',
  'led, checked, and value.",\n      valueKind: "simple",\n      validateName: (match) => match.suffix !== void 0 && safeDyna',
  'micAttribute(match.suffix),\n      renderable: (match, value) => match.suffix === void 0 ? void 0 : { type: "attribute", ',
  "attribute: match.suffix, expression: value }\n    }\n  ];\n  var genui0DirectiveInstructionLines = genui0DirectiveDefinitio",
  "ns.map(\n    (definition) => definition.instruction\n  );\n  var genui0DirectiveUsages = genui0DirectiveDefinitions.map(\n  ",
  '  (definition) => definition.usage\n  );\n  var matchesPattern = (pattern, normalizedName) => {\n    if (pattern.type === "',
  'exact") {\n      return normalizedName === pattern.name ? { name: normalizedName } : void 0;\n    }\n    if (!normalizedNam',
  "e.startsWith(pattern.prefix)) return void 0;\n    const suffix = normalizedName.slice(pattern.prefix.length);\n    return ",
  "suffix.length === 0 ? void 0 : { name: normalizedName, suffix };\n  };\n  var findDirective = (name) => {\n    const normal",
  "ized = name.toLowerCase();\n    for (const definition of genui0DirectiveDefinitions) {\n      const match = matchesPattern",
  "(definition.pattern, normalized);\n      if (match !== void 0) return { definition, match };\n    }\n    return void 0;\n  }",
  ";\n  var genui0RenderableDirectiveFromAttribute = ({\n    name,\n    value\n  }) => {\n    const directive = findDirective(na",
  "me);\n    if (directive === void 0) return void 0;\n    if (directive.definition.validateName?.(directive.match) === false",
  ") return void 0;\n    return directive.definition.renderable?.(directive.match, value);\n  };\n\n  // src/dom/sandbox-runtim",
  "e.ts\n  var installSandboxRuntime = (config2, language, global) => {\n    let nextCallId = 1;\n    let resizeObserver;\n    ",
  "const state = {};\n    const boundElements = [];\n    const directives = [];\n    const eachBlocks = [];\n    const elementS",
  'copes = /* @__PURE__ */ new WeakMap();\n    const emptyScope = {};\n    const isRecord = (value) => typeof value === "obje',
  'ct" && value !== null && !Array.isArray(value);\n    const hasOwn = (value, key) => Object.prototype.hasOwnProperty.call(',
  'value, key);\n    const readOwnProperty = (value, key) => {\n      if (typeof value !== "object" || value === null) return',
  " { found: false };\n      const descriptor = Object.getOwnPropertyDescriptor(value, key);\n      return descriptor !== voi",
  'd 0 && "value" in descriptor ? { found: true, value: descriptor.value } : { found: false };\n    };\n    const statePath =',
  ' (expression) => {\n      const source = expression.startsWith("$") ? expression.slice(1) : expression;\n      return sour',
  'ce.split(".").filter((part) => part.length > 0);\n    };\n    const readPath = (path, scope = emptyScope) => {\n      const',
  ' [name, ...rest] = path;\n      if (name === void 0) return "";\n      let value = hasOwn(scope, name) ? scope[name] : has',
  'Own(state, name) ? state[name] : "";\n      for (const property of rest) {\n        const next = readOwnProperty(value, pr',
  'operty);\n        if (!next.found) return "";\n        value = next.value;\n      }\n      return value;\n    };\n    const wr',
  "itePath = (path, value) => {\n      const [name, ...rest] = path;\n      if (name === void 0) return;\n      if (rest.lengt",
  "h === 0) {\n        state[name] = value;\n        return;\n      }\n      let cursor;\n      const current = state[name];\n   ",
  "   if (isRecord(current)) {\n        cursor = current;\n      } else {\n        cursor = {};\n        state[name] = cursor;\n",
  "      }\n      for (const property of rest.slice(0, -1)) {\n        const child = cursor[property];\n        if (isRecord(c",
  "hild)) {\n          cursor = child;\n          continue;\n        }\n        const next = {};\n        cursor[property] = nex",
  "t;\n        cursor = next;\n      }\n      const last = rest.at(-1);\n      if (last !== void 0) cursor[last] = value;\n    }",
  ";\n    const readStateFromScope = (scope = emptyScope) => (expression) => readPath(statePath(expression), scope);\n    con",
  "st evaluate = (expression, scope = emptyScope) => language.evaluateExpression(expression, readStateFromScope(scope));\n  ",
  "  const isTruthy = (value) => value !== language.invalid && value !== false && value !== null && value !== void 0 && val",
  'ue !== "" && value !== 0;\n    const textValue = (value) => {\n      if (value === language.invalid || value === null || v',
  'alue === void 0) return "";\n      if (typeof value === "string") return value;\n      if (typeof value === "number" || ty',
  'peof value === "boolean") return String(value);\n      return JSON.stringify(value);\n    };\n    const post = (message) =>',
  " {\n      global.parent.postMessage(\n        { channel: config2.channel, surfaceId: config2.surfaceId, ...message },\n    ",
  '    "*"\n      );\n    };\n    const createCallId = () => global.crypto !== void 0 && typeof global.crypto.randomUUID === "',
  'function" ? global.crypto.randomUUID() : `call-${nextCallId++}`;\n    const readElementValue = (element) => {\n      if (g',
  "lobal.HTMLInputElement !== void 0 && element instanceof global.HTMLInputElement) {\n        const type = element.type.toL",
  'owerCase();\n        if (type === "checkbox") return element.checked;\n        if (type === "radio") return element.checke',
  'd ? element.value : "";\n        if ((type === "number" || type === "range") && element.value !== "") {\n          const n',
  "umberValue = Number(element.value);\n          return Number.isFinite(numberValue) ? numberValue : element.value;\n       ",
  " }\n        return element.value;\n      }\n      if (global.HTMLSelectElement !== void 0 && element instanceof global.HTML",
  "SelectElement) {\n        if (element.multiple) return Array.from(element.selectedOptions).map((option) => option.value);",
  "\n        return element.value;\n      }\n      if (global.HTMLTextAreaElement !== void 0 && element instanceof global.HTML",
  'TextAreaElement) {\n        return element.value;\n      }\n      return element.textContent ?? "";\n    };\n    const writeE',
  "lementValue = (element, value) => {\n      if (global.HTMLInputElement !== void 0 && element instanceof global.HTMLInputE",
  'lement) {\n        const type = element.type.toLowerCase();\n        if (type === "checkbox") {\n          element.checked ',
  "= Boolean(value);\n          return;\n        }\n        element.value = textValue(value);\n        return;\n      }\n      if",
  " (global.HTMLSelectElement !== void 0 && element instanceof global.HTMLSelectElement) {\n        const values = Array.isA",
  "rray(value) ? value.map(textValue) : [textValue(value)];\n        for (const option of element.options) option.selected =",
  " values.includes(option.value);\n        return;\n      }\n      if (global.HTMLTextAreaElement !== void 0 && element insta",
  "nceof global.HTMLTextAreaElement) {\n        element.value = textValue(value);\n        return;\n      }\n    };\n    const h",
  "asAuthoredFormValue = (element) => {\n      if (global.HTMLInputElement !== void 0 && element instanceof global.HTMLInput",
  'Element) {\n        const type = element.type.toLowerCase();\n        return type === "checkbox" || type === "radio" ? ele',
  'ment.hasAttribute("checked") : element.hasAttribute("value");\n      }\n      if (global.HTMLTextAreaElement !== void 0 &&',
  " element instanceof global.HTMLTextAreaElement) {\n        return element.textContent !== null && element.textContent.len",
  "gth > 0;\n      }\n      if (global.HTMLSelectElement !== void 0 && element instanceof global.HTMLSelectElement) {\n       ",
  ' return Array.from(element.options).some((option) => option.hasAttribute("selected"));\n      }\n      return false;\n    }',
  ";\n    const closestWithAttribute = (target, attributeName) => {\n      let element = target instanceof global.Element ? t",
  "arget : null;\n      while (element !== null) {\n        if (element.hasAttribute(attributeName)) return element;\n        ",
  "element = element.parentElement;\n      }\n      return null;\n    };\n    const isBoundElement = (target) => target instanc",
  "eof global.Element && target.hasAttribute(genui0AttributeNames.bind);\n    const elementStyle = (element) => {\n      retu",
  "rn element.style;\n    };\n    const applyAttributeValue = (element, attribute, value) => {\n      if (value === language.i",
  "nvalid || value === false || value === null || value === void 0) {\n        element.removeAttribute(attribute);\n        r",
  'eturn;\n      }\n      element.setAttribute(attribute, value === true ? "" : textValue(value));\n    };\n    const applyStyl',
  "eValue = (element, property, value) => {\n      const style = elementStyle(element);\n      if (style === void 0 || !isSaf",
  "eStyleProperty(property)) return;\n      if (value === language.invalid || value === false || value === null || value ===",
  " void 0) {\n        style.removeProperty(property);\n        return;\n      }\n      const styleValue = textValue(value);\n  ",
  "    if (!isSafeStyleValue(styleValue)) {\n        style.removeProperty(property);\n        return;\n      }\n      style.set",
  "Property(property, styleValue);\n    };\n    const currentDirectives = () => [\n      ...directives,\n      ...eachBlocks.fl",
  "atMap((block) => block.directives)\n    ];\n    const refreshDirectives = () => {\n      for (const directive of currentDir",
  'ectives()) {\n        const value = evaluate(directive.expression, directive.scope);\n        if (directive.type === "text',
  '") {\n          directive.element.textContent = textValue(value);\n          continue;\n        }\n        if (directive.typ',
  'e === "show") {\n          const style = elementStyle(directive.element);\n          if (style !== void 0) {\n            s',
  'tyle.display = isTruthy(value) ? directive.visibleDisplay ?? "" : "none";\n          }\n          continue;\n        }\n    ',
  '    if (directive.type === "class_toggle") {\n          directive.element.classList.toggle(directive.className, isTruthy(',
  'value));\n          continue;\n        }\n        if (directive.type === "class_value") {\n          const dynamicClass = ty',
  'peof value === "string" ? value.trim() : "";\n          directive.element.className = dynamicClass.length === 0 ? directi',
  've.baseClassName ?? "" : [directive.baseClassName ?? "", dynamicClass].filter((item) => item.length > 0).join(" ");\n    ',
  '      continue;\n        }\n        if (directive.type === "style_property") {\n          applyStyleValue(directive.element',
  ', directive.property, value);\n          continue;\n        }\n        if (directive.type === "style_map") {\n          if (',
  "isRecord(value)) {\n            for (const [property, propertyValue] of Object.entries(value)) {\n              applyStyle",
  "Value(directive.element, property, propertyValue);\n            }\n          }\n          continue;\n        }\n        apply",
  "AttributeValue(directive.element, directive.attribute, value);\n      }\n    };\n    const reportHeight = () => {\n      con",
  'st root = global.document.documentElement;\n      const body = global.document.body;\n      post({ type: "resize", height:',
  " Math.max(root.scrollHeight, body?.scrollHeight ?? 0) });\n    };\n    const refresh = () => {\n      renderEachBlocks();\n ",
  "     refreshDirectives();\n      reportHeight();\n    };\n    const resultTargetFor = (action) => action.target ?? language",
  ".defaultResultTarget(action.capability);\n    const setResultState = (target, state2) => {\n      global.__genuiResults = ",
  "global.__genuiResults ?? {};\n      global.__genuiResults[target] = state2;\n      writePath([target], state2);\n    };\n   ",
  " const pendingResultState = (target) => {\n      const previous = readPath([target]);\n      return isRecord(previous) && ",
  'hasOwn(previous, "value") ? { status: "pending", value: previous.value } : { status: "pending" };\n    };\n    const postC',
  "apabilityCall = (expression, scope) => {\n      const action = language.parseCapabilityExpression(expression, readStateFr",
  "omScope(scope));\n      if (action === void 0) return false;\n      const target = resultTargetFor(action);\n      setResul",
  'tState(target, pendingResultState(target));\n      refresh();\n      post({\n        type: "capability",\n        callId: cr',
  "eateCallId(),\n        capability: action.capability,\n        input: action.input,\n        ...action.target === void 0 ? ",
  "{} : { target: action.target }\n      });\n      return true;\n    };\n    const runLocalAction = (expression, scope) => {\n ",
  "     const action = language.parseSetExpression(expression, readStateFromScope(scope));\n      if (action === void 0) ret",
  "urn false;\n      writePath(action.path, action.value);\n      refresh();\n      return true;\n    };\n    const scopeForElem",
  "ent = (element) => {\n      let current = element;\n      while (current !== null) {\n        const scope = elementScopes.g",
  "et(current);\n        if (scope !== void 0) return scope;\n        current = current.parentElement;\n      }\n      return e",
  "mptyScope;\n    };\n    const runAuthoredAction = (expression, scope) => runLocalAction(expression, scope) || postCapabili",
  "tyCall(expression, scope);\n    const handleClick = (event) => {\n      const action = closestWithAttribute(event.target, ",
  "genui0AttributeNames.onClick);\n      const expression = action?.getAttribute(genui0AttributeNames.onClick) ?? null;\n    ",
  "  if (action !== null && expression !== null && runAuthoredAction(expression, scopeForElement(action))) {\n        event.",
  "preventDefault();\n        return;\n      }\n      const target = event.target;\n      if (!(target instanceof global.Elemen",
  't)) return;\n      const link = target.closest("a[href]");\n      if (link === null) return;\n      const href = global.HTM',
  'LAnchorElement !== void 0 && link instanceof global.HTMLAnchorElement ? link.href : link.getAttribute("href");\n      if ',
  '(href === null) return;\n      event.preventDefault();\n      post({ type: "link", href });\n    };\n    const handleSubmit ',
  "= (event) => {\n      const target = event.target;\n      if (!(target instanceof global.Element)) return;\n      const for",
  'm = target.closest("form");\n      if (form === null || !form.hasAttribute(genui0AttributeNames.onSubmit)) return;\n      ',
  "event.preventDefault();\n      const expression = form.getAttribute(genui0AttributeNames.onSubmit);\n      if (expression ",
  "!== null) runAuthoredAction(expression, scopeForElement(form));\n    };\n    const handleBoundInput = (event) => {\n      c",
  "onst target = event.target;\n      if (!isBoundElement(target)) return;\n      writePath(\n        statePath(target.getAttr",
  'ibute(genui0AttributeNames.bind) ?? ""),\n        readElementValue(target)\n      );\n      refresh();\n    };\n    const han',
  "dleResultMessage = (event) => {\n      const message = event.data;\n      if (!isRecord(message)) return;\n      if (messag",
  'e.channel !== config2.channel || message.surfaceId !== config2.surfaceId) return;\n      if (message.type !== "result" ||',
  ' typeof message.target !== "string") return;\n      setResultState(message.target, message.state);\n      refresh();\n    }',
  ";\n    const installInitialState = () => {\n      for (const element of global.document.querySelectorAll(`[${genui0Attribu",
  "teNames.state}]`)) {\n        const parsed = language.parseObjectLiteral(\n          element.getAttribute(genui0AttributeN",
  'ames.state) ?? "{}",\n          readStateFromScope(emptyScope)\n        );\n        if (!isRecord(parsed)) continue;\n      ',
  "  for (const [name, value] of Object.entries(parsed)) state[name] = value;\n      }\n    };\n    const installBindings = ()",
  " => {\n      for (const element of global.document.querySelectorAll(`[${genui0AttributeNames.bind}]`)) {\n        const pa",
  'th = statePath(element.getAttribute(genui0AttributeNames.bind) ?? "");\n        if (path.length === 0) continue;\n        ',
  'boundElements.push({ element, path });\n        if (hasAuthoredFormValue(element) || readPath(path) === "") {\n          w',
  "ritePath(path, readElementValue(element));\n        } else {\n          writeElementValue(element, readPath(path));\n      ",
  "  }\n      }\n    };\n    const installEachBlocks = () => {\n      const isNestedEachBlock = (element) => {\n        let pare",
  "nt = element.parentElement;\n        while (parent !== null) {\n          if (parent.hasAttribute(genui0AttributeNames.eac",
  "h)) return true;\n          parent = parent.parentElement;\n        }\n        return false;\n      };\n      const elements ",
  "= Array.from(\n        global.document.querySelectorAll(`[${genui0AttributeNames.each}]`)\n      ).filter((element) => !is",
  "NestedEachBlock(element));\n      for (const element of elements) {\n        const expression = element.getAttribute(genui",
  "0AttributeNames.each);\n        if (expression === null) continue;\n        const itemName = element.getAttribute(genui0At",
  'tributeNames.as) ?? "item";\n        const template = Array.from(element.childNodes).map((node) => node.cloneNode(true));',
  "\n        element.replaceChildren();\n        eachBlocks.push({ element, expression, itemName, template, directives: [] })",
  ";\n      }\n    };\n    const installDirective = (element, attribute, scope, targetDirectives) => {\n      const directive =",
  " genui0RenderableDirectiveFromAttribute(attribute);\n      if (directive === void 0) return;\n      targetDirectives.push(",
  '{\n        ...directive,\n        element,\n        visibleDisplay: directive.type === "show" ? elementStyle(element)?.disp',
  'lay ?? "" : void 0,\n        baseClassName: directive.type === "class_value" ? element.className : void 0,\n        scope\n',
  "      });\n    };\n    const installDirectives = (root, scope, targetDirectives) => {\n      const elements = root instance",
  'of global.Element ? [root, ...Array.from(root.querySelectorAll("*"))] : Array.from(root.querySelectorAll("*"));\n      fo',
  "r (const element of elements) {\n        for (const attribute of element.attributes) {\n          installDirective(element",
  ", attribute, scope, targetDirectives);\n        }\n      }\n    };\n    const renderElementTree = (element, scope, targetDir",
  "ectives) => {\n      elementScopes.set(element, scope);\n      for (const attribute of element.attributes) {\n        insta",
  "llDirective(element, attribute, scope, targetDirectives);\n      }\n      if (element.hasAttribute(genui0AttributeNames.ea",
  "ch)) {\n        renderEachElement(element, scope, targetDirectives);\n        return;\n      }\n      for (const child of el",
  "ement.children) renderElementTree(child, scope, targetDirectives);\n    };\n    const renderEachTemplate = (element, expre",
  "ssion, itemName, template, parentScope, targetDirectives) => {\n      element.replaceChildren();\n      const items = eval",
  "uate(expression, parentScope);\n      if (!Array.isArray(items)) return;\n      for (const item of items) {\n        const ",
  "itemScope = { ...parentScope, [itemName]: item };\n        for (const templateNode of template) {\n          const clone =",
  " templateNode.cloneNode(true);\n          element.append(clone);\n          if (clone instanceof global.Element) renderEle",
  "mentTree(clone, itemScope, targetDirectives);\n        }\n      }\n    };\n    function renderEachElement(element, scope, ta",
  "rgetDirectives) {\n      const expression = element.getAttribute(genui0AttributeNames.each);\n      if (expression === nul",
  'l) return;\n      const itemName = element.getAttribute(genui0AttributeNames.as) ?? "item";\n      const template = Array.',
  "from(element.childNodes).map((node) => node.cloneNode(true));\n      renderEachTemplate(element, expression, itemName, te",
  "mplate, scope, targetDirectives);\n    }\n    const renderEachBlocks = () => {\n      for (const block of eachBlocks) {\n   ",
  "     block.directives = [];\n        renderEachTemplate(\n          block.element,\n          block.expression,\n          b",
  "lock.itemName,\n          block.template,\n          emptyScope,\n          block.directives\n        );\n      }\n    };\n    ",
  "installInitialState();\n    installEachBlocks();\n    installBindings();\n    installDirectives(global.document, emptyScope",
  ', directives);\n    renderEachBlocks();\n    refreshDirectives();\n    global.addEventListener("load", reportHeight);\n    g',
  'lobal.document.addEventListener("click", handleClick);\n    global.document.addEventListener("submit", handleSubmit);\n   ',
  ' global.document.addEventListener("input", handleBoundInput);\n    global.document.addEventListener("change", handleBound',
  'Input);\n    global.addEventListener("message", handleResultMessage);\n    if (global.ResizeObserver !== void 0 && global.',
  "document.body !== null) {\n      resizeObserver = new global.ResizeObserver(reportHeight);\n      resizeObserver.observe(g",
  "lobal.document.body);\n    }\n    return {\n      dispose() {\n        resizeObserver?.disconnect();\n        global.removeEv",
  'entListener("load", reportHeight);\n        global.document.removeEventListener("click", handleClick);\n        global.doc',
  'ument.removeEventListener("submit", handleSubmit);\n        global.document.removeEventListener("input", handleBoundInput',
  ');\n        global.document.removeEventListener("change", handleBoundInput);\n        global.removeEventListener("message"',
  ", handleResultMessage);\n      }\n    };\n  };\n\n  // src/dom/sandbox-entry.ts\n  var sandboxGlobal = globalThis;\n  var confi",
  "g = sandboxGlobal.__genuiSandboxConfig;\n  delete sandboxGlobal.__genuiSandboxConfig;\n  if (config === void 0) {\n    thro",
  'w new Error("Missing GenUI sandbox config.");\n  }\n  var genui0Language2 = createGenui0Language();\n  installSandboxRuntim',
  "e(\n    config,\n    {\n      invalid: genui0Language2.invalid,\n      parseObjectLiteral: (source, readState) => genui0Lang",
  "uage2.parseObjectLiteral(source, readState),\n      evaluateExpression: (source, readState) => genui0Language2.evaluateEx",
  "pression(source, readState),\n      parseCapabilityExpression: (expression, readState) => genui0Language2.parseCapability",
  "Expression(expression, readState),\n      parseSetExpression: (expression, readState) => genui0Language2.parseSetExpressi",
  "on(expression, readState),\n      defaultResultTarget: (capability) => genui0Language2.defaultResultTarget(capability)\n  ",
  "  },\n    sandboxGlobal\n  );\n})();\n",
] as const

export const sandboxRuntimeAsset = sandboxRuntimeAssetChunks.join("")
